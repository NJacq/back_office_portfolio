{% extends 'base.html.twig' %}

{% block title %}Informations{% endblock %}

{% block body %}
<a class="text-muted" href="{{ path('logout') }}">Déconnexion</a>

<div class="nav">
    <a class="lienPages" href="{{ path('projets_index') }}">Projets</a>
    <a class="lienPages" href="{{ path('images_index') }}">Images</a>
    <a class="lienPages" href="{{ path('competences_index') }}">Compétences</a>
    <a class="lienPages" href="{{ path('informations_index') }}">Informations</a>
</div>
    <h1>Informations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ information.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ information.description }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ information.tel }}</td>
            </tr>
            <tr>
                <th>Tel_interna</th>
                <td>{{ information.telInterna }}</td>
            </tr>
            <tr>
                <th>Cv_url</th>
                <td>{{ information.cvUrl }}</td>
            </tr>
        </tbody>
    </table>
<p>

<div class="commande1">
    <a class="stylebouton1" href="{{ path('informations_edit', {'id': information.id}) }}">Modifier</a>
    {{ include('informations/_delete_form.html.twig') }}</p>
</div>
<div>
    <a href="{{ path('informations_index') }}">Retour à la liste</a>
</div>
{% endblock %}