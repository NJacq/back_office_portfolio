{% extends 'base.html.twig' %}

{% block title %}Compétences{% endblock %}

{% block body %}
    <h1>Compétences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ competence.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ competence.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('competences_index') }}">Retour à la liste</a>

    <a href="{{ path('competences_edit', {'id': competence.id}) }}">Modifier</a>

    {{ include('competences/_delete_form.html.twig') }}
{% endblock %}