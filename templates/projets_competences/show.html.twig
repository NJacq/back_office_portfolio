{% extends 'base.html.twig' %}

{% block title %}ProjetsCompetences{% endblock %}

{% block body %}
    <h1>ProjetsCompetences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projets_competence.id }}</td>
            </tr>
            <tr>
                <th>Id_projet</th>
                <td>{{ projets_competence.idProjet }}</td>
            </tr>
            <tr>
                <th>Id_competence</th>
                <td>{{ projets_competence.idCompetence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projets_competences_index') }}">Retour à la liste</a>

    <a href="{{ path('projets_competences_edit', {'id': projets_competence.id}) }}">Modifier</a>

    {{ include('projets_competences/_delete_form.html.twig') }}
{% endblock %}