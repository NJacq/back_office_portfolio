{% extends 'base.html.twig' %}

{% block title %}Projets{% endblock %}

{% block body %}
    <h1>Projets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ projet.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ projet.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ projet.image }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ projet.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projets_index') }}">Retour à la liste</a>

    <a href="{{ path('projets_edit', {'id': projet.id}) }}">Modifier</a>

    {{ include('projets/_delete_form.html.twig') }}
{% endblock %}