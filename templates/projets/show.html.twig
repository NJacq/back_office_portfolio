{% extends 'base.html.twig' %}

{% block title %}Projets{% endblock %}

{% block body %}
    <div>
        <a class="text-muted" href="{{ path('logout') }}">Déconnexion</a>
    </div>
    <div  class="nav">
    <a class="lienPages" href="{{ path('projets_index') }}">Projets</a>
    <a class="lienPages" href="{{ path('images_index') }}">Images</a>
    <a class="lienPages" href="{{ path('competences_index') }}">Compétences</a>
    <a class="lienPages" href="{{ path('informations_index') }}">Informations</a>
    </div>
    <h1>Projets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ projet.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ projet.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ projet.image }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ projet.adresse }}</td>
            </tr>
        </tbody>
    </table>
    <td>
        <div class="commande1">
            <a class="stylebouton1" href="{{ path('projets_edit', {'id': projet.id}) }}">Modifier</a>
            {{ include('projets/_delete_form.html.twig') }}
        </div>
    </td>
    <div>
        <a href="{{ path('projets_index') }}">Retour à la liste</a>
    </div>
{% endblock %}