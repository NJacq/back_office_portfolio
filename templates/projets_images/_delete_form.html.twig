<form method="post" action="{{ path('projets_images_delete', {'id': projets_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projets_image.id) }}">
    <button class="btn">Supprimer</button>
</form>