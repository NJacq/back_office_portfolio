{% extends 'base.html.twig' %}

{% block title %}ProjetsImages{% endblock %}

{% block body %}
    <h1>ProjetsImages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projets_image.id }}</td>
            </tr>
            <tr>
                <th>Id_projet</th>
                <td>{{ projets_image.idProjet }}</td>
            </tr>
            <tr>
                <th>Id_image</th>
                <td>{{ projets_image.idImage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projets_images_index') }}">Retour à la liste</a>

    <a href="{{ path('projets_images_edit', {'id': projets_image.id}) }}">Modifier</a>

    {{ include('projets_images/_delete_form.html.twig') }}
{% endblock %}